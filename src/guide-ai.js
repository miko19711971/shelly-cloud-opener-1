// src/guide-ai.js
// Modulo di placeholder per il Guest Assistant.
// Al momento non cambia nulla: l'endpoint /api/guest-assistant
// continua a funzionare esattamente come definito in server.js.

export default function guideAI(app) {
  // Nessuna logica per ora.
  // In futuro potremo spostare qui l'intelligenza che legge i JSON
  // e genera le risposte, senza toccare il resto del server.
}
